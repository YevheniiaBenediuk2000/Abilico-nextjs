# Storage Bucket для фото місць

## Пояснення: Storage Bucket vs JSONB колонка

### Як працює система фото:

1. **Supabase Storage Bucket `place-photos`**
   - Це сховище для **самих файлів** (зображень)
   - Містить реальні .jpg, .png файли
   - Має бути створений в Supabase Dashboard → Storage → Buckets
   - Має бути **Public** (щоб фото були доступні через URL)

2. **Колонка `photos` (jsonb) в таблиці `places`**
   - Це тільки для збереження **URLs** (посилань) на фото
   - Зберігає масив рядків, наприклад:
     ```json
     [
       "https://your-project.supabase.co/storage/v1/object/public/place-photos/photo1.jpg",
       "https://your-project.supabase.co/storage/v1/object/public/place-photos/photo2.jpg"
     ]
     ```

### Процес завантаження:

1. Користувач вибирає фото в формі
2. Файл завантажується в Storage bucket `place-photos`
3. Отримуємо public URL файлу
4. Зберігаємо URL в колонку `photos` (jsonb) таблиці `places`

### Чому потрібен Storage Bucket?

- **Файли не можна зберігати в базі даних** - це було б неефективно
- **Storage** - це окреме сховище для файлів (як S3)
- **JSONB колонка** - це тільки для метаданих (URLs)

## Як створити Storage Bucket:

1. Зайдіть в **Supabase Dashboard**
2. Перейдіть в **Storage** → **Buckets**
3. Натисніть **New bucket**
4. Назва: `place-photos` (точно так!)
5. Поставте галочку **Public bucket** (Public: Yes)
6. Натисніть **Create bucket**

Після цього завантаження фото працюватиме автоматично! 📸

